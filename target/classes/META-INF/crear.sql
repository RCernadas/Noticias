CREATE TABLE Noticia(titulo VARCHAR(50) NOT NULL PRIMARY KEY,autor VARCHAR(50),fecha DATE);
CREATE TABLE Comentario(id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,texto VARCHAR(80),autor VARCHAR(30),noticia_titulo VARCHAR(50));
ALTER TABLE COMENTARIO ADD CONSTRAINT  noticia_titulo FOREIGN KEY(noticia_titulo) REFERENCES Noticia(titulo);